# --- Brains: Qwen3.6-35B-A3B via a Modal managed inference endpoint (OpenAI-compatible) ---
OPENAI_BASE_URL=https://<workspace>--<endpoint>.modal.direct/v1
OPENAI_API_KEY=modal                 # dummy; the Modal endpoint authenticates via the headers below
MODAL_KEY=wk-...
MODAL_SECRET=ws-...
MODEL=Qwen/Qwen3.6-35B-A3B
TICK_SECONDS=90                      # loop.py only; pulse_loop.py pulses continuously

# --- State: Supabase (anon key works because schema.sql + fix_rls.sql leave RLS off) ---
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_KEY=eyJ...

# --- Real-world actions: Manus ---
MANUS_API_KEY=sk-...

# --- WhatsApp intake: Wassist (used to verify the X-Wassist-Signature header; optional) ---
WASSIST_SIGNING_SECRET=...
